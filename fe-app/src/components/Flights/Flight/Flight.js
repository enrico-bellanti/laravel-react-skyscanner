import React from "react";
import { Typography, Paper } from "@material-ui/core";

const Flight = (flight, stepovers) => {
  console.log(flight);
  return (
    <Paper>
      <Typography>Flight</Typography>
    </Paper>
  );
};

export default Flight;
